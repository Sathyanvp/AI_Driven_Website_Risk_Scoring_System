# Spring Boot Configuration for Phishing Detection Service

spring:
  application:
    name: phishing-detection-service
  
  # Server configuration
  server:
    port: 8080
#    servlet:
#      context-path: /
#  
  # Logging configuration
  # This will create a file named 'app.log' in a folder called 'logs' 
  # located in your project's root directory.
  logging:
    file:
      name: logs/app.log
    level:
      root: INFO
      phishing_website: DEBUG
      ai.onnxruntime: WARN
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

## Phishing Detection Configuration
model:
  # Path to ONNX model file
  path: src/main/resources/model/phishing_xgboost.onnx
#  
  # Feature configuration
#  features:
#    url:
#      max-length: 200
#    entropy:
#      max-char: 6.0
#      max-token: 5.0
#      max-ngram: 6.0
#    dom:
#      max-forms: 10
#      max-iframes: 10

# Server settings
server:
  tomcat:
    threads:
      max: 200
      min-spare: 10
  connection-timeout: 20000